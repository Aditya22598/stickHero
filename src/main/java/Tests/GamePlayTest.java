package Tests;

import GameLayers.GamePlay;
import javafx.scene.layout.AnchorPane;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;

import java.util.concurrent.atomic.AtomicReference;

import static org.junit.jupiter.api.Assertions.*;
import static org.testng.Assert.assertTrue;

public class GamePlayTest {

//        @Test
//        void testLegalStickLength() {
//                // Instantiate GamePlay
//                AnchorPane gamePlay = GamePlay.getInstance();
//
//                // Test case 1: Expected to be true
//                AtomicReference<Integer> firstPillarIndex1 = new AtomicReference<>(0);
//                Assertions.assertTrue(GamePlay.legalStickLength(firstPillarIndex1), "Expected true for first pillar index 0");
//
//                // Test case 2: Expected to be false
//                AtomicReference<Integer> firstPillarIndex2 = new AtomicReference<>(1);
//                assertFalse(GamePlay.legalStickLength(firstPillarIndex2), "Expected false for first pillar index 1");
//
//                // Add more test cases as necessary
//        }
}
